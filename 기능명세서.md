# 껌딱지월드 서버 구현 명세서

> **갱신 (2026)**: 본 문서는 **REST API + 인메모리 상태** 기준으로 정리했습니다. **Socket.io는 사용하지 않습니다.** 요청/응답 필드의 정본은 **[API.md](./API.md)** 입니다.

## 📋 프로젝트 개요

졸업전시 "껌딱지월드"의 백엔드 서버

* **역할**: 모니터 2대에 대한 할당·대기열·체험 완료 상태 관리
* **기술**: Node.js + Express (HTTP/JSON), **Socket.io 없음**
* **배포**: Railway / Render 등 (환경에 맞게)
* **DB**: Supabase (PostgreSQL) — 전시 콘텐츠·스토리지용 (모니터 큐 상태는 서버 메모리)

---

## 🛠️ 기술 스택

### Core

* **Runtime**: Node.js 20.x LTS 권장
* **Framework**: Express 4.x
* **통신**: REST (태블릿·모니터 클라이언트는 `fetch` 등으로 호출)
* **Database Client**: @supabase/supabase-js 2.x

### Dependencies (개략)

* `express`, `cors`, `dotenv`, `@supabase/supabase-js`
* 개발: `jest`, `supertest`, `nodemon`

상세 버전은 `package.json`을 따릅니다.

### 환경변수

```bash
PORT=3000
NODE_ENV=production
CORS_ORIGIN=https://your-frontend.example
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_ANON_KEY=...
```

---

## 📊 Supabase DB 스키마

### worries 테이블

```sql
-- 고민 데이터 저장
CREATE TABLE worries (
  id BIGSERIAL PRIMARY KEY,
  worry_image_url TEXT NOT NULL,           -- Storage 파일명 (예: "1735392000.jpg")
  worry_text TEXT,                         -- 고민 내용 (선택사항)
  submitted_at TIMESTAMPTZ DEFAULT NOW(),  -- 제출 시간
  displayed_at TIMESTAMPTZ,                -- 빔프로젝터 표시 시간
  monitor_id TEXT,                         -- 체험한 모니터 ("monitor-1" or "monitor-2")
  status TEXT DEFAULT 'displaying',        -- 상태 ("displaying", "completed")
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 인덱스 (빠른 조회)
CREATE INDEX idx_worries_submitted_at ON worries(submitted_at DESC);
CREATE INDEX idx_worries_status ON worries(status);

-- RLS (Row Level Security) 비활성화 (공개 데이터)
ALTER TABLE worries ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Enable read access for all users" ON worries FOR SELECT USING (true);
CREATE POLICY "Enable insert access for all users" ON worries FOR INSERT WITH CHECK (true);
CREATE POLICY "Enable update access for all users" ON worries FOR UPDATE USING (true);

-- Realtime 활성화
ALTER PUBLICATION supabase_realtime ADD TABLE worries;
```

### Storage 버킷

```sql
-- Storage 버킷 설정 (Supabase 대시보드에서 생성)
Bucket Name: worry-images
Public: true (공개 접근)
File Size Limit: 1MB
Allowed MIME types: image/jpeg, image/png
```

---

## 🎯 기능적 요구사항

### 1. 모니터 할당 시스템 (REST)

#### 1-1. 모니터 상태 (메모리)

* 대상: `monitor-1`, `monitor-2`
* 각각 `idle` | `busy`
* 구조 개념:

```javascript
{
  'monitor-1': {
    status: 'idle' | 'busy',
    currentWorry: null | { worryId, assignedAt, svgUrl, sessionId }, // Stage3 진행 중
    reservedWorry: null | { worryId, svgUrl, sessionId },             // 태블릿/대기열에서 붙었으나 Stage3 미시작
    clientId: null | string
  },
  'monitor-2': { ... }
}
```

#### 1-2. 할당 로직

* **입력**: `POST /api/request-monitor` 바디의 `worryId`, 선택 `displaySeq`(방문객 문구용 순번), `svgUrl`, `sessionId`, `clientId`
* **처리**:
  1. **빈 모니터**(idle + `reservedWorry` 없음)가 있으면 `reserve` → 응답 `assigned: true` + 안내 메시지 (서버는 아직 **`busy` 아님**)
  2. 없으면 대기열 `add(clientId, …)` → 응답 `assigned: false` + `queuePosition` + `clientId`

#### 1-3. Stage3 시작

* **입력**: `POST /api/monitors/:monitorId/start`
* **처리**: `reservedWorry` → `currentWorry`, **`busy`**

#### 1-4. 체험 완료 (Stage6)

* **입력**: `POST /api/monitors/:monitorId/complete`
* **처리**: `release(monitorId)` → `dequeue()` → 다음 사람 있으면 같은 모니터에 **`reserve`만** (즉시 `busy` 아님). 다음 세션 `busy`는 다시 **`start`**

### 2. 대기열 관리

* FIFO, 항목당 **약 5분** 타임아웃 후 제거 및 `onTimeout` 콜백 (서버 로그 등)
* 대기열 항목 식별자: **`clientId`** (태블릿이 넘기거나 서버가 `anonymous-…` 발급)
* **참고**: 소켓 연결 끊김 시 자동 제거는 하지 않음. 순번 갱신은 클라이언트가 `GET /api/queue/position`으로 폴링.

### 3. 헬스·운영

* `GET /health` — 상태 요약
* `GET /ping` — 경량 생존 확인
* `GET /status` — 디버깅용 상세 (모니터 객체·대기열 길이)

---

## ⚡ 비기능적 요구사항

* **응답 시간**: 할당 API는 로컬/동일 리전에서 수십 ms 단위를 목표
* **동시성**: 인메모리 단일 프로세스 기준; 다중 인스턴스로 늘리면 큐가 분산되므로 전시 단일 서버 전제에 맞게 운영
* **메모리**: 모니터 2대·대기열 길이에 비례, 일반적으로 매우 작음

---

## 🔄 주요 플로우 (REST)

### 할당 성공

```
[태블릿]  POST /api/request-monitor          [서버]                    [모니터]
              ──────────────────────────────▶ findAvailable → reserve
              ◀──────────────────────────────  { assigned: true, monitorId }
                                                                    │
                                         POST .../start (Stage3) ────┘
                                         GET .../current → { busy, worry }
```

### 할당 대기

```
[태블릿]  POST /api/request-monitor     [서버]
              ────────────────────────▶  모두 busy → queue.add
              ◀────────────────────────  { assigned: false, queuePosition, clientId }

[태블릿]  GET /api/queue/position?clientId=...  (폴링, 선택)
```

### 체험 완료 후 다음 사람

```
[모니터]  POST .../complete              [서버]
              ────────────────────────▶  release → dequeue → reserve
              ◀────────────────────────  { ok, assignedNext }

[모니터]  다음 세션: POST .../start 후 busy → … → complete
```

### 서버 재시작

* 메모리 상태 초기화 → 모니터·태블릿은 API 재호출·폴링으로 재동기화

---

## 📡 REST API (개요)

엔드포인트·에러·예제는 **[API.md](./API.md)** 에서 관리합니다.

| 메서드 | 경로 | 용도 |
|--------|------|------|
| POST | `/api/monitor-instance/bind` | 동일 URL 모니터 → `monitor-1`/`2` 슬롯 배정 (`instanceId`) |
| GET | `/api/monitor-instance/bind?instanceId=` | 기존 배정 조회 |
| POST | `/api/request-monitor` | 태블릿 배정 요청 |
| GET | `/api/monitors/:monitorId/current` | 모니터 폴링 |
| POST | `/api/monitors/:monitorId/start` | Stage3 시작 → busy |
| POST | `/api/monitors/:monitorId/complete` | Stage6 종료 → idle, 다음 사람 예약 |
| GET | `/api/queue/position` | 대기 순번 (`0` = 없음) |
| GET | `/health`, `/ping`, `/status` | 운영·디버그 |

**연동 클라이언트(별도 레포)** — 서버 계약은 위·`API.md`가 정본이며, 단말 구현·`seq`/Edge 등 세부는 아래 계획서에 둡니다.

* **태블릿 (tablet-entry-card)**: [CHANGE_PLAN_tablet-entry-card.md](./CHANGE_PLAN_tablet-entry-card.md)
* **모니터 (gum-frontend)**: [CHANGE_PLAN_gum-frontend.md](./CHANGE_PLAN_gum-frontend.md)
* **서버 흐름 요약**: [docs/MONITOR_USER_FLOW.md](./docs/MONITOR_USER_FLOW.md)

---

## 📁 프로젝트 구조 (현재)

```
gum_server/
├── server.js                 # createApp(), 진입 시 listen
├── API.md
├── src/
│   ├── config/
│   │   └── supabase.js
│   ├── managers/
│   │   ├── MonitorManager.js
│   │   ├── MonitorBindingManager.js
│   │   ├── QueueManager.js
│   │   └── __tests__/
│   └── utils/
│       ├── logger.js
│       └── constants.js
├── __tests__/
│   └── integration.test.js   # supertest REST 통합
└── postman/                  # (선택) 컬렉션
```

---

## 🔧 핵심 모듈

* **`MonitorManager`**: `findAvailable`, `reserve`, `start`, `release`, `getStatus`. `reservedWorry` / `currentWorry` / `clientId` 보관.
* **`MonitorBindingManager`**: `instanceId` → `monitor-1`/`monitor-2` 바인딩(동일 URL 대응).
* **`QueueManager`**: `add(clientId, worryId, onTimeout, { svgUrl, sessionId })`, `remove`, `dequeue`, `getPosition`, `getAllClientIds`, `clear`.
* **`server.js`**: 라우트만 정의; 비즈니스 로직은 매니저에 위임.

구현 세부·코드는 **레포 소스**가 정본입니다.

---

## 🧪 테스트

```bash
npm test                 # 단위 + REST 통합
npm run test:client      # 서버 기동 후 REST 시나리오 (test-client.js)
npm run test:api         # 헬스 등 간단 GET
```

수동 확인: Postman — **[docs/POSTMAN_GUIDE.md](./docs/POSTMAN_GUIDE.md)**

---

## 🚀 배포

* Node 프로세스: `npm start` (`node server.js`)
* 환경변수에 `PORT`, Supabase 키, 필요 시 `CORS_ORIGIN` 설정
* Render 요약: **[docs/RENDER_DEPLOY.md](./docs/RENDER_DEPLOY.md)**

---

## 🔒 보안·운영 체크리스트

* [ ] `.env` 커밋 제외
* [ ] 프로덕션 CORS를 실제 프론트 도메인으로 제한
* [ ] 에러 응답에 내부 비밀 노출 금지
* [ ] HTTPS (호스팅 기본값 활용)

---

## ✅ 완료 체크리스트 (REST 기준)

* [x] MonitorManager / QueueManager
* [x] REST 라우트 (`monitor-instance/bind`, `request-monitor`, `current`, `start`, `complete`, `queue/position`)
* [x] 헬스·상태 엔드포인트
* [x] Jest + supertest 통합 테스트
* [ ] 프로덕션 CORS 최종 확정
* [ ] 모니터·태블릿 클라이언트가 API.md와 동일 계약으로 연동 확인

---

## 🎓 학습 키워드

* Express 라우팅·JSON 바디
* 인메모리 상태와 단일 프로세스 한계
* FIFO 큐·타임아웃(`setTimeout`/`clearTimeout`)
* `fetch` / HTTP 폴링 패턴
* Supabase Storage·Realtime (프론트·다른 서비스와 연계 시)

---

질문·변경은 **API.md**와 실제 코드를 우선 확인한 뒤 본 문서를 맞추면 됩니다.
